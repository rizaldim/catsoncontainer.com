{% extends "layout.njk" %}

{% block content %}
<h1>{{ title }}</h1>

<p class="postDate">{{ page.date | postDate }}</p>
<ul role="list" class="tagsList">
  {% for item in tags %}
  <li>{{ item }}</li>
  {% endfor %}
</ul>
{{ content | safe }}
{% endblock %}
